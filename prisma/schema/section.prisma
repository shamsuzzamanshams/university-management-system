model Section {
  id           String       @id @default(uuid())
  name         String
  capacity     Int
  roomNumber   String?
  schedule     String
  semesterId   String
  instructorId String
  attendances  Attendance[]
  exams        Exam[]


  @@unique([semesterId, name])
  @@map("section")
}
