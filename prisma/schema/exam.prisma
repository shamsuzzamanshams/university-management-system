model Exam {
  id        String       @id @default(uuid())
  title     String
  maxMarks  Float
  weightage Float
  examDate  DateTime
  sectionId String
  results   ExamResult[]
  section   Section      @relation(fields: [sectionId], references: [id])

  @@map("exam")
}
