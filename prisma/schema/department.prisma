model Department {
  id          String       @id @default(uuid())
  name        String       @unique
  code        String       @unique
  courses     Course[]
  instructors Instructor[]
  programs    Program[]
  students    Student[]

  @@map("department")
}
